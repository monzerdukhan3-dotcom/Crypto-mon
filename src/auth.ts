// Lets code import the NextAuth helpers as "@/auth" (the Auth.js v5
// convention) as well as "@/lib/auth", where the config itself lives.
export { auth, handlers, signIn, signOut } from "@/lib/auth";
